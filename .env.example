# Export these variables in your shell before starting the application.
# Never commit a real secret key.
SECRET_KEY=replace-with-a-long-random-development-secret

# Optional. The default is instance/humanframe.db.
# DATABASE_URL=sqlite:////absolute/path/to/humanframe.db

# Set to true only when serving the application over HTTPS.
SESSION_COOKIE_SECURE=false
