# Hardware

Put GPIO, serial, audio-device, servo, and other platform-specific adapters here.
High-level robot code should depend on small interfaces rather than importing
hardware libraries directly.
