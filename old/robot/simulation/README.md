# Simulation

Add simulated microphones, speakers, displays, motors, and environments here.
Simulators should satisfy the same small interfaces as physical hardware, but no
device abstractions are implemented in the baseline application.
